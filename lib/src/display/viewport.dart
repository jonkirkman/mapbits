
part of mapbits;

/**
 * A top-level component encapsulating a CRS that layers & features can be added to.
 * Each [Viewport] is also responsible for notifying it's dependents of changes
 * that effect the visible scope, such as panning, zooming, resizing.
 */
class Viewport {
  Viewport();
}